{{>birthday}}
<div class="text-container">
    <h1>Sign our petition to give everone in Berlin their birthday off!</h1>
    <h3>
        (Don't worry: If your birthday that year is on a national holiday, you
        will get an additional day instead)
    </h3>
</div>

<div class="container">
    <form method="POST">
        {{#if error}}
        <h2 class="hidden">Something went wrong. Please try again</h2>
        {{/if}}

      

        <input type="hidden" name="_csrf" value="{{ csrfToken }}" />

        <input type="hidden" name="signature" id="signature" required />
        <canvas id="canvas-signature"></canvas>
        <p>Sign here</p>

        <button type="submit">Submit</button>
    </form>
</div>
